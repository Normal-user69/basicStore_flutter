import 'package:flutter/material.dart';

class AppIcons{
  Icon get search => const Icon(Icons.search);
  Icon get list => const Icon(Icons.list , size: 40,);
  Icon get home => const Icon(Icons.home , size: 40,);
  Icon get settings => const Icon(Icons.settings , size: 40,);
  Icon get store => const Icon(Icons.store , size: 40,);
}